{
  "name": "my-typescript-app",
  "version": "1.0.0",
  "description": "A TypeScript app that uses the Jest test framework",
  "scripts": {
    "build": "tsc",
    "start": "node ./dist/src/index.js",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4",
    "typescript": "^4.4.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}